Performance improvments

1: Write bitmaps to canvas to only send one image to liveview for perfect performace
Bitmap b = Bitmap.createBitmap(100, 100, Bitmap.Config.ARGB_8888);
Canvas c = new Canvas(b);